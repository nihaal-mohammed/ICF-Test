#frontend
npm install
npm start

#backend 
github.com/marketplace/models/azure-openai/gpt-4o-mini/playground 
    - click use this model
    - click create personal access token
    - in the top left click tokens (classic)
    - if needed click generate new token
    - then click generate new token (classic)
    - write a note for the token then create
    - copy the token/api key and replace it for api_key
pip install
    - do this command in powershell
python vector_db_pipline.py
    - do this command in powershell
$env:GITHUB_TOKEN = “api_key”
    - do this command in powershell
python ollamaLLM.py
    - do this command in powershell